<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Corona Seniors</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css"
        integrity="sha384-9aIt2nRpC12Uk9gS9baDl411NQApFmC26EwAOH8WgZl5MYYxFfc+NcPb1dKGj7Sk" crossorigin="anonymous" />
</head>

<body>

    <% if(alerts.error) { %>
    <% alerts.error.forEach(message => { %>
    <div class="alert alert-danger mb-0">
        <%= message %>
    </div>
    <% })} %>

    <% if(alerts.success) { %>
    <% alerts.success.forEach(message => { %>
    <div class="alert alert-success mb-0">
        <%= message %>
    </div>
    <% })} %>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        
        
    </button>
    
    <a class="navbar-brand" href="/">Corona </a>
    <a class="navbar-brand" href="/senior"> Seniors</a>
    <a class="navbar-brand" href="/helper"> Helpers</a>
    <a class="navbar-brand" href="/auth/signup"> Sign Up</a>

            <% if(currentUser) { %>
                <a style="float: right"class="nav-link" href="/auth/signout">Sign Out</a>
            <% } else { %>
                <a style="float: right" class="nav-link" href="/auth/signin">Sign In</a>
            <% } %>

        </div>

    </nav>
    <%- body %>
</body>

</html>